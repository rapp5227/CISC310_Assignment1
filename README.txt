pointers.cpp should be compiled using the compile.sh script included in this repository

The program will take files as input as specified in the Canvas documentation (i.e. "./pointers < input.txt > output.txt"), or by user prompts from the command line.